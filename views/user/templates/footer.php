    </main>
    <footer class="relative bg-cream overflow-hidden mt-auto" style="border-top: 1.5px solid rgba(122, 91, 67, 0.15) !important;">
      
      <div class="absolute inset-0 pointer-events-none z-0 opacity-[0.032] mix-blend-multiply bg-[url('data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%22140%22 height=%22140%22%3E%3Cfilter id=%22n%22%3E%3CfeTurbulence type=%22fractalNoise%22 baseFrequency=%220.8%22 numOctaves=%223%22 stitchTiles=%22stitch%22/%3E%3C/filter%3E%3Crect width=%22140%22 height=%22140%22 filter=%22url(%23n)%22/%3E%3C/svg%3E')] bg-[size:140px_140px]"></div>

      <div class="absolute top-1/2 left-[35%] -translate-x-1/2 -translate-y-1/2 w-[480px] h-[280px] bg-radial from-pink-blush/7 via-transparent to-transparent blur-[48px] pointer-events-none z-0"></div>

      <div class="absolute right-0 bottom-0 w-[230px] h-[280px] pointer-events-none z-0 opacity-25">
        <svg viewBox="0 0 200 250" fill="none" xmlns="http://www.w3.org/2000/svg" class="w-full h-full">
          <g stroke="#8b7d74" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M200 220 C170 190 140 130 160 60 C165 40 175 20 180 10" />
            <path d="M180 10 C160 15 145 5 135 -5 C150 5 168 8 180 10 Z" />
            <path d="M180 10 C185 10 190 2 195 -5 C185 0 182 5 180 10 Z" />
            <path d="M172 40 C145 42 120 30 105 20 C125 28 150 36 172 40 Z" />
            <path d="M172 40 C155 35 138 28 105 20" />
            <path d="M166 55 C185 62 205 75 215 90 C195 80 180 70 166 55 Z" />
            <path d="M166 55 C180 62 195 72 215 90" />
            <path d="M160 80 C125 85 95 70 75 55 C100 68 135 76 160 80 Z" />
            <path d="M160 80 C138 74 112 66 75 55" />
            <path d="M156 105 C180 115 200 130 215 150 C188 135 170 122 156 105 Z" />
            <path d="M156 105 C172 115 190 128 215 150" />
          </g>
        </svg>
      </div>

      <div class="relative z-10 max-w-[1500px] mx-auto px-3 sm:px-5 lg:px-6 py-10">
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-10 xl:gap-14">
          
          <div class="flex flex-col gap-3">
            <a href="index.php?action=home" class="flex items-center gap-2.5 text-left focus:outline-none cursor-pointer group w-max text-decoration-none">
              <img src="assets/images/logo_spadmin.png" alt="Logo SPAdmin" class="w-11 h-11 object-contain shadow-sm rounded-full">
              <div class="flex flex-col gap-0.5 leading-none">
                <span class="font-sans font-bold text-xl tracking-wider text-brown-soft">SPADMIN</span>
                <span class="font-cursive italic text-lg font-semibold" style="color: #db83a6 !important;">Wellness</span>
              </div>
            </a>
            <div class="w-7 h-[2px] bg-brown-soft/60 rounded-full mt-1"></div>
            <p class="font-sans text-base line-clamp-3 text-brown-dark leading-relaxed font-medium mb-0">
              Ruang relaksasi hangat untuk tubuh yang lelah dan pikiran yang membutuhkan ketenangan restoratif sejenak.
            </p>
            <div class="flex gap-2.5 mt-2">
              <a href="#" class="w-8 h-8 rounded-full bg-[#f5ebe6] text-brown-dark hover:bg-brown-soft hover:text-white transition-all flex items-center justify-center">
                <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="w-3.5 h-3.5"><rect x="2" y="2" width="20" height="20" rx="5" ry="5"></rect><path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"></path><line x1="17.5" y1="6.5" x2="17.51" y2="6.5"></line></svg>
              </a>
              <a href="#" class="w-8 h-8 rounded-full bg-[#f5ebe6] text-brown-dark hover:bg-brown-soft hover:text-white transition-all flex items-center justify-center">
                <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="w-3.5 h-3.5"><line x1="22" y1="2" x2="11" y2="13"></line><polygon points="22 2 15 22 11 13 2 9 22 2"></polygon></svg>
              </a>
            </div>
          </div>

          <div class="flex flex-col lg:border-l lg:border-brown-soft/20 lg:pl-6">
            <h4 class="font-sans font-extrabold text-[15px] tracking-widest text-[#a48c71] mb-2 mt-0">ALAMAT</h4>
            <div class="w-7 h-[2px] bg-brown-soft/60 rounded-full mb-3"></div>
            <div class="flex items-start gap-3">
              <span class="w-6 h-6 rounded-full bg-brown-soft/15 text-brown-soft flex items-center justify-center shrink-0 mt-0.5">
                <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="w-3 h-3"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"></path><circle cx="12" cy="10" r="3"></circle></svg>
              </span>
              <span class="font-sans text-base text-brown-dark/80 leading-relaxed font-semibold">
                Jl. Setiabudi No. 123<br />Bandung, Jawa Barat
              </span>
            </div>
          </div>

          <div class="flex flex-col lg:border-l lg:border-brown-soft/20 lg:pl-6">
            <h4 class="font-sans font-extrabold text-[15px] tracking-widest text-[#a48c71] mb-2 mt-0">KONTAK</h4>
            <div class="w-7 h-[2px] bg-brown-soft/60 rounded-full mb-3"></div>
            <div class="flex flex-col gap-3">
              <div class="flex items-start gap-3">
                <span class="w-6 h-6 rounded-full bg-brown-soft/15 text-brown-soft flex items-center justify-center shrink-0 mt-0.5">
                  <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="w-3 h-3"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"></path></svg>
                </span>
                <span class="font-sans text-base text-brown-dark/80 leading-relaxed font-semibold">0812-3456-7890</span>
              </div>
              <div class="flex items-start gap-3">
                <span class="w-6 h-6 rounded-full bg-brown-soft/15 text-brown-soft flex items-center justify-center shrink-0 mt-0.5">
                  <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="w-3 h-3"><rect x="2" y="4" width="20" height="16" rx="2"></rect><path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"></path></svg>
                </span>
                <span class="font-sans text-base text-brown-dark/80 leading-relaxed font-semibold break-all">halo@spadmin.test</span>
              </div>
            </div>
          </div>

          <div class="flex flex-col lg:border-l lg:border-brown-soft/20 lg:pl-6">
            <h4 class="font-sans font-extrabold text-[15px] tracking-widest text-[#a48c71] mb-2 mt-0">JAM BUKA</h4>
            <div class="w-7 h-[2px] bg-brown-soft/60 rounded-full mb-3"></div>
            <div class="flex items-start gap-3">
              <span class="w-6 h-6 rounded-full bg-brown-soft/15 text-brown-soft flex items-center justify-center shrink-0 mt-0.5">
                <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="w-3 h-3"><circle cx="12" cy="12" r="10"></circle><polyline points="12 6 12 12 16 14"></polyline></svg>
              </span>
              <span class="font-sans text-base text-brown-dark/80 leading-relaxed font-semibold">
                Senin – Minggu <br />09.00 – 20.00 WIB
              </span>
            </div>
          </div>

        </div>

        <div class="relative border-t border-brown-soft/15 pt-6 pb-2 mt-8 flex flex-col items-center">
          
          <div class="absolute -top-3.5 left-1/2 -translate-x-1/2 bg-cream px-3.5 text-brown-soft transition-transform hover:scale-110 duration-300">
            <img src="assets/images/logo_spadmin.png" alt="Logo SPAdmin" class="w-[30px] h-[30px] object-contain rounded-full">
          </div>

          <p class="font-sans text-[15px] font-bold text-brown-dark tracking-wide mt-2 mb-0">
            &copy; <?= date('Y') ?> SPAdmin Spa Bandung. All rights reserved.
          </p>
        </div>
      </div>
    </footer>
    <script>
    function closeFloatingAlert() {
        document.querySelectorAll('.floating-alert').forEach((alertEl) => {
            alertEl.classList.remove('show');
        });
    }

    document.addEventListener('DOMContentLoaded', () => {
        const alerts = document.querySelectorAll('.floating-alert');
        if (alerts.length) {
            setTimeout(() => {
                alerts.forEach((alertEl) => alertEl.classList.add('show'));
            }, 150);

            setTimeout(() => {
                alerts.forEach((alertEl) => alertEl.classList.remove('show'));
            }, 4000);
        }

        bindAjaxCartForms();
        initPasswordControls();
    });

    function showCartToast(message, success = true, tone = '') {
        let alertEl = document.getElementById('ajaxCartAlert');
        if (!alertEl) {
            alertEl = document.createElement('div');
            alertEl.id = 'ajaxCartAlert';
            alertEl.className = 'floating-alert';
            alertEl.innerHTML = `
                <div class="alert-icon"></div>
                <div class="alert-message"></div>
                <button class="alert-close" type="button" aria-label="Tutup notifikasi">&times;</button>
            `;
            document.body.appendChild(alertEl);
            alertEl.querySelector('.alert-close').addEventListener('click', () => {
                alertEl.classList.remove('show');
            });
        }

        const alertTone = tone || (success ? 'success' : 'error');
        alertEl.classList.remove('success', 'error', 'danger', 'show');
        alertEl.classList.add(alertTone);
        alertEl.querySelector('.alert-icon').innerHTML = success
            ? '<svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path><polyline points="22 4 12 14.01 9 11.01"></polyline></svg>'
            : '<svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"></circle><line x1="12" y1="8" x2="12" y2="12"></line><line x1="12" y1="16" x2="12.01" y2="16"></line></svg>';
        alertEl.querySelector('.alert-message').textContent = message;

        requestAnimationFrame(() => alertEl.classList.add('show'));
        clearTimeout(window.ajaxCartToastTimer);
        window.ajaxCartToastTimer = setTimeout(() => {
            alertEl.classList.remove('show');
        }, 4000);
    }

    function updateCartBadges(count) {
        if (count > 0 && !document.querySelector('.nav-cart-count')) {
            const desktopIcon = document.querySelector('header a[href="index.php?action=keranjang"] .relative');
            const mobileCart = document.querySelector('header .md\\:hidden a[href="index.php?action=keranjang"]');

            if (desktopIcon) {
                const badge = document.createElement('span');
                badge.className = 'nav-cart-count absolute -top-1 -right-1 min-w-4 h-4 rounded-full bg-brown-dark text-[9px] font-sans font-bold text-white flex items-center justify-center px-1 border border-cream animate-pulse';
                desktopIcon.appendChild(badge);
            }

            if (mobileCart) {
                const badge = document.createElement('span');
                badge.className = 'nav-cart-count absolute top-0 right-0 min-w-4 h-4 rounded-full bg-brown-dark text-[8px] font-sans font-bold text-white flex items-center justify-center px-1 border border-cream';
                mobileCart.appendChild(badge);
            }
        }

        const badges = document.querySelectorAll('.nav-cart-count');
        badges.forEach((badge) => {
            badge.textContent = count;
            badge.classList.toggle('d-none', count <= 0);
        });
    }

    function updateSubmittedCartForm(form, data) {
        const button = form.querySelector('button[type="submit"]');
        if (!button || !data.success) return;

        if (data.mode === 'add') {
            form.setAttribute('action', 'index.php?action=hapus-keranjang');
            form.dataset.cartConfirm = '';
            form.dataset.confirmTitle = 'Hapus layanan?';
            form.dataset.confirmMessage = 'Layanan ini akan dihapus dari keranjang Anda.';
            form.dataset.confirmLabel = 'Hapus';
            button.innerHTML = '<svg xmlns="http://www.w3.org/2000/svg" width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="3 6 5 6 21 6"></polyline><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"></path><line x1="10" y1="11" x2="10" y2="17"></line><line x1="14" y1="11" x2="14" y2="17"></line></svg>Hapus';
            button.classList.add('is-in-cart');
            button.classList.add('is-added');
        } else if (data.mode === 'remove') {
            form.setAttribute('action', 'index.php?action=tambah-keranjang');
            form.removeAttribute('data-cart-confirm');
            delete form.dataset.confirmTitle;
            delete form.dataset.confirmMessage;
            delete form.dataset.confirmLabel;
            button.innerHTML = '<svg xmlns="http://www.w3.org/2000/svg" width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="9" cy="21" r="1"></circle><circle cx="20" cy="21" r="1"></circle><path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"></path></svg>Keranjang';
            button.classList.remove('is-in-cart');
            button.classList.remove('is-added');
        }
    }

    function initPasswordControls() {
        document.querySelectorAll('[data-toggle-password]').forEach((button) => {
            const input = document.getElementById(button.getAttribute('data-toggle-password'));
            if (!input) return;

            button.addEventListener('click', () => {
                const visible = input.type === 'text';
                input.type = visible ? 'password' : 'text';
                button.classList.toggle('is-visible', !visible);
                button.setAttribute('aria-label', visible ? 'Tampilkan password' : 'Sembunyikan password');
            });
        });

        document.querySelectorAll('[data-password-rules]').forEach((input) => {
            const list = document.querySelector('[data-password-requirements="' + input.id + '"]');
            if (!list) return;
            list.querySelector('[data-rule="max"]')?.remove();

            const updateRules = () => {
                const value = input.value;
                const rules = {
                    min: value.length >= 8,
                    uppercase: /[A-Z]/.test(value),
                    special: /[^A-Za-z0-9]/.test(value),
                };
                list.classList.toggle('is-visible', value.length > 0);

                Object.keys(rules).forEach((rule) => {
                    const item = list.querySelector('[data-rule="' + rule + '"]');
                    if (item) item.classList.toggle('is-valid', rules[rule]);
                });
            };

            input.addEventListener('input', updateRules);
            input.addEventListener('focus', updateRules);
            input.addEventListener('blur', updateRules);
            updateRules();
        });
    }

    function bindAjaxCartForms() {
        document.addEventListener('submit', async (event) => {
            const form = event.target;
            if (!(form instanceof HTMLFormElement)) return;

            const action = form.action || form.getAttribute('action') || '';
            const isAdd = action.includes('action=tambah-keranjang');
            const isRemove = action.includes('action=hapus-keranjang');
            const isCartPage = new URLSearchParams(window.location.search).get('action') === 'keranjang';

            if (!isAdd && !isRemove) return;
            if (isRemove && isCartPage) return;

            if (isRemove && form.hasAttribute('data-cart-confirm') && form.dataset.confirmed !== '1') {
                event.preventDefault();
                openGlobalCartConfirm(form);
                return;
            }

            if (form.dataset.confirmed === '1') {
                delete form.dataset.confirmed;
            }

            event.preventDefault();

            const button = form.querySelector('button[type="submit"]');
            if (button) button.disabled = true;

            try {
                const response = await fetch(action, {
                    method: 'POST',
                    body: new FormData(form),
                    headers: {
                        'X-Requested-With': 'XMLHttpRequest',
                        'Accept': 'application/json',
                    },
                });
                const data = await response.json();

                updateCartBadges(Number(data.cart_count || 0));
                updateSubmittedCartForm(form, data);
                showCartToast(data.message || 'Keranjang diperbarui.', Boolean(data.success), isRemove && data.success ? 'danger' : '');
            } catch (error) {
                showCartToast('Keranjang belum bisa diperbarui. Coba lagi sebentar.', false);
            } finally {
                if (button) button.disabled = false;
            }
        });
    }

    function openGlobalCartConfirm(form) {
        let modal = document.getElementById('globalCartConfirmModal');
        if (!modal) {
            modal = document.createElement('div');
            modal.id = 'globalCartConfirmModal';
            modal.className = 'cart-confirm-modal global-cart-confirm-modal';
            modal.hidden = true;
            modal.setAttribute('role', 'dialog');
            modal.setAttribute('aria-modal', 'true');
            modal.setAttribute('aria-labelledby', 'globalCartConfirmTitle');
            modal.setAttribute('aria-describedby', 'globalCartConfirmMessage');
            modal.innerHTML = `
                <div class="cart-confirm-dialog">
                    <div class="cart-confirm-icon" aria-hidden="true">
                        <svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round"><path d="M12 9v4"></path><path d="M12 17h.01"></path><path d="M10.29 3.86 1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0Z"></path></svg>
                    </div>
                    <h2 class="cart-confirm-title" id="globalCartConfirmTitle">Konfirmasi</h2>
                    <p class="cart-confirm-message" id="globalCartConfirmMessage">Apakah Anda yakin ingin melanjutkan?</p>
                    <div class="cart-confirm-actions">
                        <button type="button" class="cart-confirm-cancel" data-confirm-cancel>Batal</button>
                        <button type="button" class="cart-confirm-accept" data-confirm-accept>Ya, lanjutkan</button>
                    </div>
                </div>
            `;
            document.body.appendChild(modal);
        }

        const title = modal.querySelector('#globalCartConfirmTitle');
        const message = modal.querySelector('#globalCartConfirmMessage');
        const cancel = modal.querySelector('[data-confirm-cancel]');
        const accept = modal.querySelector('[data-confirm-accept]');

        title.textContent = form.dataset.confirmTitle || 'Konfirmasi';
        message.textContent = form.dataset.confirmMessage || 'Apakah Anda yakin ingin melanjutkan?';
        accept.textContent = form.dataset.confirmLabel || 'Ya, lanjutkan';
        modal.hidden = false;
        requestAnimationFrame(() => modal.classList.add('is-open'));
        cancel.focus();

        const close = () => {
            modal.classList.remove('is-open');
            setTimeout(() => {
                if (!modal.classList.contains('is-open')) modal.hidden = true;
            }, 180);
        };

        cancel.onclick = close;
        modal.onclick = (event) => {
            if (event.target === modal) close();
        };
        accept.onclick = () => {
            close();
            form.dataset.confirmed = '1';
            form.requestSubmit();
        };
    }
    </script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
